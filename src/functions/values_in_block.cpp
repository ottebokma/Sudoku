#include "sudoku.ih"

vector<int> values_in_column(Sudoku const &sudoku, size_t const column)
{
    vector<int> values;

    for (size_t i = 0; i < count; i++)
    {
        /* code */
    }
    return values;
}